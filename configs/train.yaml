# Best-run recipe. Edit root/output; relative paths are relative to this YAML.
# Fresh training to a maximum of 320 epochs (early stopping remains enabled).
active_probability: 0.85
activity_attempts: 12
activity_frame_duration: 0.2
activity_threshold: 0.001
amp: auto
batch_size: 7
checkpoint: null
coherent_mix_probability: 0.1
diagnostic_every: 2
diagnostic_train_tracks: 14
diagnostic_train_windows: 2
diagnostic_uniform_windows: 4
diagnostic_workers: 2
early_stop_min_delta: 0.0
ema_decay: 0.999
epochs: 320
grad_accum_steps: 1
grad_clip: 5.0
init_checkpoint: null
init_scope: all-compatible
lr: 0.0002
lr_decay_cooldown: 2
lr_decay_gamma: 0.5
lr_decay_patience: 8
lr_min: 1.25e-05
lr_threshold: 0.01
lr_threshold_mode: abs
max_quiet_frame_fraction: 0.05
min_active_frame_fraction: 0.3
nb_workers: 8
nfft: 4096
nhop: 1024
no_cuda: false
no_ema: false
no_pin_memory: false
no_random_track_mix: false
output: ../runs/vocals
patience: 25
prefetch_factor: 4
remix_gain_db: 3.0
root: ../data/musdb18
sample_rate: 44100
samples_per_track: 18
seed: 42
seq_dur: 11.0
valid_probe_windows: 24
valid_tracks: 14
valid_windows_per_track: 4
validation_manifest: validation_tracks.json
weight_decay: 1.0e-05
win_length: 4096
device: auto
